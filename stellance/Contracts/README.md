# Contracts

This folder contains Soroban smart contract examples for Stellance.

- `src/` - contract source code
- `tests/` - contract unit tests
- `Cargo.toml` - contract metadata and dependencies
- `tsconfig.json` - TypeScript config for contract workspace tools
